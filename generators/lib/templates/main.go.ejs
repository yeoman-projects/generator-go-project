// +build sample

package main

import (
	"fmt"

	"github.com/<%- author %>/<%- projectName %>"
)

func main() {
	fmt.Printf("A version of the Package %s is %s\n", "<%- projectName %>", <%- projectName %>.Version())
}
