// +build sample

package main

import (
	"fmt"

	"github.com/<%- author %>/<%- project_name %>"
)

func main() {
	fmt.Printf("A version of the Package %s is %s\n", "<%- project_name %>", <%- project_name %>.Version())
}
