.PHONY: gen lint test<% if (type === 'CLI') { %> install man<% } else if (type === 'Library') { %> sample<% } %>

VERSION := `git vertag get`
COMMIT  := `git rev-parse HEAD`

gen:
	go generate ./...

lint: gen
	golangci-lint run

test: lint
	go test -v --race ./...
<%_ if (type === 'CLI') { _%>

install: test
	go install -a -ldflags "-X=main.version=$(VERSION) -X=main.commit=$(COMMIT)" ./...

man:
	go run main.go --help-man <%- ">" %> <%- name %>.1
<%_ } else if (type === 'Library') { _%>

sample:
	go run -tags=sample ./cmd/<%- name %>-sample/main.go
<%_ } _%>
