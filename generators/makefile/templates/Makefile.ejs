.PHONY: gen lint test<% if (project_type === 'CLI') { %> install man<% } else if (project_type === 'Library') { %> sample<% } %>

VERSION := `git vertag get`
COMMIT  := `git rev-parse HEAD`

gen:
	go generate ./...

lint: gen
	golangci-lint run

test: lint
	go test -v --race ./...
<%_ if (project_type === 'CLI') { _%>

install: test
	go install -a -ldflags "-X=main.version=$(VERSION) -X=main.commit=$(COMMIT)" ./...

man:
	go run main.go --help-man <%- ">" %> <%- project_name %>.1
<%_ } else if (project_type === 'Library') { _%>

sample:
	go run -tags=sample ./cmd/<%- project_name %>-sample/main.go
<%_ } _%>
