package main

import (
	"os"

	"github.com/alecthomas/kingpin"
)

// nolint
var (
	version = "snapshot"
	commit  = "snapshot"
	date    = "snapshot"
)

func main() {
	app := kingpin.New("<%= projectName %>", "<%= description %>").Version(version).Author("<%= author %>")
	switch kingpin.MustParse(app.Parse(os.Args[1:])) {
	}
}
